# ── OpenAI (optional) ───────────────────────────────────────────────
# Use this name only — never OPENAI_* with a VITE_ prefix. Vite exposes every
# VITE_* variable to browser code at build time, which would leak secrets.
OPENAI_API_KEY=
